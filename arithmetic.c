#include<stdio.h>
#include<conio.h>
int main()
{
 int first,dif,term,value,sum=0,i;
 printf("Enter the number of terms in AP series");
 scanf("%d",&terms );
 printf("Enter first term and common difference of AP series ");
 scanf("%d%d",&firs,&diff);
 value=first;
 for(i=0;i<terms;i++)
 {
  printf("%d",value);
  sum f=value;
  value=value+diff
  }
  printf(sum of AP series fill %d term is %d ",terms,sum);
  getch() ;
  return 0;
 }
