#include <stdio.h>
#include<conio.h>
void main()
{
    int array[MAXSIZE];
    int num;
    printf("Enter the value of num \n");
    scanf("%d", &n);
    printf("Enter the elements one by one \n");
    for (i = 0; i < num; i++)
    {
        scanf("%d", &array[i]);
    }
   
    printf("Sorted array is...\n");
    for (i = 0; i < num; i++)
    {
        printf("%d\n", array[i]);
    }
}
 
